git --version       : github versiyon kontrolü
git config --global user.name "Yaman Aktaş"     : github kullanıcı adını belirler.
git config --global user.email "yaman.aktas@bahcesehir.edu.tr" : github kullanıcı emailini belirler.
cd path     :bu komut verilen path içerisine ilerliyor.
git clone link      : verilen linkteki github reposunu bilgisayara indirir.
python3 --version       : python versiyon kontrolü
python3 -m venv cv_matching        : python yeni bir sanal geliştirme ortamı oluşturur.
source cv_matching/bin/activate        : sanal geliştirme ortamını aktif ediyor.
ls -la      :klasör içindeki bütün contenti yetkileriyle beraber listeler
touch dosya_adı.dosya_uzantısı      : dosya oluşturur.
mkdir klasör_adı        : klasör oluşturuyor 
git add.        :değişiklik yapılan şeyleri listeler
git commit -m       : değişiklik listesine başlık ekler
git push        :githuba yükleme yapar